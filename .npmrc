# vitest@5 declares a peer of @types/node >=22, but the project pins @types/node@^20.
# The conflict is cosmetic for our usage, so tolerate it during install (npm ci/install
# on Vercel would otherwise abort with ERESOLVE before the build runs).
legacy-peer-deps=true
